# backend の開発・テスト時だけ使う依存（本番イメージには入れない）
# 実行時依存も含めてインストールするため、先頭で requirements.txt を読み込む。
#   pip install -r requirements-dev.txt

-r requirements.txt

ruff==0.16.6                 # Lint ＋ フォーマッタ（黒 + flake8 相当を 1 つで）
mypy==2.3.1                  # 静的型チェック
pytest==9.1.1               # テストランナー
pytest-asyncio==1.4.0       # async なテスト関数を動かす
pytest-cov==7.1.0           # カバレッジ計測（行 ＋ 分岐）
httpx==0.28.1               # テストから FastAPI を叩く HTTP クライアント（ASGITransport）
