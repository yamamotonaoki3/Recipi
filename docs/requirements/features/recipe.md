# レシピ CRUD

## 1. 目的・概要

レシピの作成・閲覧・編集・削除。レシピはタイトル・説明・何人分・材料・手順・**サムネイル画像（1 枚）**・**手順ごとの画像（各 1 枚・任意）**・公開フラグからなる。更新・削除は投稿者本人のみ。

関連: 単位は [unit.md](unit.md)、画像は [image.md](image.md)、感想は [comment.md](comment.md)、一覧フィードは [home-feed.md](home-feed.md)、検索は [search.md](search.md)。

## 2. 画面・UI

### レシピ詳細（見る）画面

上から順に（画面設計は [../screens/recipe-detail.md](../screens/recipe-detail.md)）:

1. **サムネイル画像 1 枚**（無ければプレースホルダ）。※ 画像カルーセルは無い
2. タイトル
3. 投稿者行（アバター + 表示名 + フォローボタン。タップでユーザープロフィール）
4. メタ情報（何人分・投稿日）
5. ♡ ボタン + お気に入り数（[favorite.md](favorite.md)）
6. 説明
7. 材料リスト（材料グループごとに表示。グループ名があれば見出し、なければフラット。各材料 = 材料名 ＋ 数量・単位。単位の前置 / 後置は [unit.md](unit.md) の表示ルール。例: `じゃがいも 3 個` / `砂糖 大さじ 2` / `塩 少々`。別レシピ参照付きの材料名はリンク）
8. 手順リスト（番号付き。各手順は本文 ＋ その手順の画像があれば画像を縦に表示）
9. 感想セクション（[comment.md](comment.md)）
- 本人が見ている場合、上部に「編集」「削除」

### レシピ作成・編集画面

**入力項目と UI・バリデーション**

| 項目 | UI | 必須 | バリデーション |
| --- | --- | --- | --- |
| サムネイル画像 | 端末のカメラ / ギャラリーから 1 枚選択。変更 / 削除可 | | [image.md](image.md) の形式・サイズ |
| タイトル | 単一行テキスト | ○ | 1〜120 文字 |
| 説明 | 複数行テキスト | | 0〜2000 文字 |
| 何人分 | 数値入力（ステッパー併設） | ○ | 整数 1〜99 |
| 材料（グループ＋行リスト） | **材料グループ**（見出し）の下に材料行が並ぶ。1 行 =「材料名 / 数量 / 単位」。`+` で行追加、各行に削除、グループ内・グループ間で並べ替え。「グループを追加」でグループ増加、グループ名は任意。各材料行は「レシピから選ぶ」で自分の別レシピへのリンクにできる（下記） | ○（材料の合計 1 件以上） | グループ名: 任意 0〜40 文字。材料名: 必須 1〜60 文字。数量: 任意、0 より大きい数値（小数可）。単位: 任意、自由入力 0〜20 文字（候補から選択も可、[unit.md](unit.md)） |
| 手順（行リスト） | 1 行 = 本文 ＋ **「画像を追加」ボタン**（その手順の画像 1 枚。差し替え / 削除可）。行頭に手順番号を自動表示。`+` で行追加、各行に削除ボタン、行の並べ替え可 | ○（1 行以上） | 本文: 必須 1〜1000 文字。画像: 任意、手順 1 行につき 1 枚 |
| 公開フラグ | トグル | ○ | 既定は非公開（OFF） |

**行編集の挙動（材料・手順共通）**

- `+` ボタンを押すと末尾に空行が追加され、入力フォーカスがそこへ移る。
- 各行の削除ボタンで行を削除する。削除後、手順の番号（`position`）は 1 起点の連番に振り直す。手順を削除するとその手順の画像も外れる。
- 並べ替えはドラッグまたは上下移動ボタンで行う（UI の最終形は実装時に決定）。並べ替え後も `position` を 1 起点の連番に正規化する。手順画像は手順に付いて一緒に動く。
- 保存時、本文が空の手順行はクライアント側で送信前に除外する。

**材料グループ**

- レシピは **1 個以上の材料グループ**を持つ。ユーザーがグループを作っていないレシピ = **名前なしグループ 1 つ**にすべての材料が入る（詳細画面では見出しなしのフラット表示になる）。
- 新規作成時の初期状態は「名前なしグループ 1 つ ＋ 空の材料行 1 つ」（従来と同じ見た目でスタート）。
- 「グループを追加」でグループを増やせる。グループ名は任意（例「合わせ調味料A」「生地B」）。グループの並べ替え・削除ができる（グループ削除で中の材料も消える → 確認ダイアログ）。
- 材料行は**グループ内での並べ替え**に加え、**ドラッグで別グループへ移動**できる。
- 保存時、材料が 0 のグループはクライアント側で送信前に除外する。グループの `position`、グループ内材料の `position` を 1 起点の連番に正規化する。
- 手順（steps）はグループ化しない。フラットな番号付きリストのまま。手順本文で「合わせ調味料A を作る」などとグループ名に言及できる。

**材料行のレシピ参照**

- 各材料行の「レシピから選ぶ」から、**自分の別レシピ**（公開 / 非公開問わず）を選ぶと、その材料行がリンク付きになる。
  - `name` にそのレシピのタイトルが 60 文字に丸めて自動入力される（ユーザー編集可）。
  - `refRecipeId` にそのレシピの ID がセットされる。数量・単位は通常どおり任意で入力できる。
- 編集中のレシピ自身は選べない（自己参照不可）。
- 「リンクを解除」で通常の材料行に戻る（`name` は残る）。
- 詳細画面ではリンク付きの材料名をタップすると参照先のレシピ詳細へ遷移する（[../screens/recipe-detail.md](../screens/recipe-detail.md)）。参照先が削除されているとリンクは無効（「このレシピは削除されました」）。

### 自分のレシピ一覧

- レシピカードの 1 リスト（公開・非公開混在、非公開に「非公開」バッジ）。**カーソルページングによる無限スクロール**（[../non-functional.md](../non-functional.md)）。
- 空状態: 「まだレシピを投稿していません」

## 3. 振る舞い・ルール

| 操作 | 認証 | 認可 |
| --- | --- | --- |
| 作成 | 必要 | ログインユーザーが投稿者になる |
| 詳細 | 任意（API） | 公開レシピは誰でも。非公開は投稿者本人のみ。他人がアクセスした場合は**存在を伏せるため 404**（403 ではなく 404。感想エンドポイントと統一）。※ 当面のアプリ画面はログイン必須で、この匿名アクセスは使わない（[../screens/navigation.md](../screens/navigation.md)） |
| 更新 | 必要 | 投稿者本人のみ（他人は 403） |
| 削除 | 必要 | 投稿者本人のみ（他人は 403）。関連する材料グループ / 材料 / 手順 / お気に入り / 感想 / 通知も削除。サムネ・手順画像はストレージ削除ジョブ対象。**自分の他レシピの材料がこのレシピを参照していた場合、その `ref_recipe_id` は SET NULL**（材料行は消えず `ref_recipe_title` が残る） |
| 自分の投稿一覧 | 必要 | 本人の公開 / 非公開すべて |

- `PUT` は全項目更新。**材料グループ（`ingredientGroups`）・手順は送られた配列で全入れ替え**（グループも材料も ID を保持しない）。
- `ingredientGroups` は 1 件以上、各グループの材料も 1 件以上、材料の合計 1 件以上。違反は 400。
- 材料の `refRecipeId` を指定できるのは、**このレシピの投稿者本人が所有するレシピ**のみ（公開 / 非公開問わず）。他ユーザーのレシピ、または編集中のレシピ自身（自己参照）を指定したら 400。サーバーは保存時に `ref_recipe_title` にそのレシピの現在のタイトルを記録する。
- 新しい画像は**先に `POST /images`（[image.md](image.md)）でアップロードして `key` を得て**、レシピの `POST` / `PUT` の body で `thumbnailKey` と各 `steps[].imageKey` に指定して紐付ける。更新時は、更新対象のレシピ自身に既に紐付いているキーも再送できる。
- サーバーは `title` から `title_normalized`、各材料 `name` から `name_normalized` を生成する（[search.md](search.md)）。
- サーバーは材料の `unit` のうち `units` 未登録のものを自動追加する（[unit.md](unit.md)）。
- 公開レシピの新規投稿時、投稿者のフォロワー全員に通知（[notification.md](notification.md) `followee_new_recipe`）。この fan-out は**作成トランザクション内で `notification_outbox` に 1 行だけ書き、コミット後に `BackgroundTasks` が配布**する（取りこぼしは定期スイープが回収。[../processing-model.md](../processing-model.md) §3・§7・§9）。実装は Phase 8。
- レシピの作成 / 更新は、`recipes` ＋ 材料グループ ＋ 材料 ＋ 手順 ＋ 未登録単位の追加 ＋ `title_normalized` / `name_normalized` 生成を**単一トランザクション**で行う。参照から外れた画像キーは削除キューへ（[../processing-model.md](../processing-model.md) §6）。

## 4. データモデル

| テーブル | 主なカラム |
| --- | --- |
| `recipes` | `id` PK / `user_id` FK（CASCADE）/ `title`（1〜120）/ `title_normalized`（index）/ `description`（0〜2000）/ `servings` NOT NULL CHECK(1〜99) / `is_public` NOT NULL / `thumbnail_key` NULL / `favorite_count` NOT NULL DEFAULT 0 / `comment_count` NOT NULL DEFAULT 0 / `created_at` / `updated_at` |
| `ingredient_groups` | `id` PK / `recipe_id` FK（CASCADE）/ `name` NULL（0〜40。NULL / 空 = 名前なし）/ `position` / UNIQUE(`recipe_id`, `position`) / UNIQUE(`id`, `recipe_id`) |
| `ingredients` | `id` PK / `recipe_id` FK（CASCADE）/ 複合 FK (`group_id`, `recipe_id`) → `ingredient_groups`(`id`, `recipe_id`)（CASCADE。材料と親グループの `recipe_id` 一致を保証）/ `name`（1〜60）/ `name_normalized`（index）/ `quantity` NUMERIC NULL CHECK(>0) / `unit` VARCHAR NULL / `ref_recipe_id` FK → `recipes.id`（**SET NULL**）NULL / `ref_recipe_title` NULL（0〜120）/ `position` / UNIQUE(`group_id`, `position`) / CHECK(`ref_recipe_id` <> `recipe_id`) |
| `steps` | `id` PK / `recipe_id` FK（CASCADE）/ `position` / `body`（1〜1000）/ `image_key` NULL / UNIQUE(`recipe_id`, `position`) |

> `recipe_images` テーブルは廃止。画像は `recipes.thumbnail_*` と `steps.image_*` に統合。
> 集約ビューは [../data-model.md](../data-model.md)。

## 5. API

### GET `/recipes/{id}`（認証任意）

- 公開レシピ: 誰でも。非公開: 本人のみ。他人がアクセスした場合は 404（存在を伏せる）
- レスポンスに `author`（id / 表示名 / アバター URL）, `isFavorited`, `favoriteCount`, `commentCount`, `thumbnailUrl`, `ingredientGroups[]`, `steps[]`（各 `body` と `imageUrl`）を含む。各画像 URL は保存されたキーからサーバーが生成する表示用の派生値
- `ingredientGroups[]` = `[{ name: string|null, ingredients: [{ name, quantity, unit, refRecipe }] }]`。
  - `refRecipe`: `{ id, title }`（参照先が生きている）/ `{ id: null, title: "<スナップショット>" }`（参照先削除済み）/ `null`（通常の材料）。

```json
// GET /recipes/{id} の材料（抜粋）
"ingredientGroups": [
  { "name": null, "ingredients": [
    { "name": "豚バラ肉", "quantity": 200, "unit": "g", "refRecipe": null },
    { "name": "自家製甜麺醤だれ", "quantity": null, "unit": "大さじ",
      "refRecipe": { "id": "a1b2c3…", "title": "自家製 甜麺醤だれ" } }
  ]},
  { "name": "合わせ調味料", "ingredients": [
    { "name": "しょうゆ", "quantity": null, "unit": "小さじ", "refRecipe": null }
  ]}
]
```

### POST `/recipes`（認証必要）

```json
{
  "title": "回鍋肉",
  "description": "定番の中華",
  "servings": 2,
  "isPublic": true,
  "thumbnailKey": "uploads/ab12…",
  "ingredientGroups": [
    { "name": null, "ingredients": [
      { "name": "豚バラ肉",       "quantity": 200,  "unit": "g",    "refRecipeId": null },
      { "name": "自家製甜麺醤だれ", "quantity": null, "unit": "大さじ", "refRecipeId": "a1b2c3…" }
    ]},
    { "name": "合わせ調味料", "ingredients": [
      { "name": "しょうゆ", "quantity": null, "unit": "小さじ", "refRecipeId": null }
    ]}
  ],
  "steps": [
    { "position": 1, "body": "合わせ調味料を混ぜておく", "imageKey": "uploads/cd34…" },
    { "position": 2, "body": "炒めて合わせ調味料をからめる", "imageKey": null }
  ]
}
// response 201: 作成された recipe オブジェクト（id 付き）
```

- `ingredientGroups` は 1 件以上。各グループの `name` は任意（null / 空 = 名前なし）、`ingredients` は 1 件以上。グループ順・材料順は配列順（`position` はサーバーが振る）。
- `ingredients[].refRecipeId` は任意（null 可）。指定するレシピは本人所有・自己参照でないこと（違反は 400）。
- `thumbnailKey` は任意（null 可）。`steps[].imageKey` も任意（null 可）。
- `key` は事前に `POST /images`（[image.md](image.md)）で取得した一時アップロードのキー。サーバーが本参照として確定する。
- `title_normalized` / `name_normalized` / `ref_recipe_title` はサーバー生成（クライアントは送らない）。

### PUT `/recipes/{id}`（認証必要、本人のみ）

- body は `POST` と同形式。**材料グループ・材料・手順は配列で全入れ替え**（ID を保持しないので、クライアントは残したい `refRecipeId` も含めて全体を送り直す）。
- 参照から外れた（＝どのレシピからも `ref_recipe_id` で参照されなくなった）状態は特に後処理不要（`ref_recipe_id` は参照する側のカラム）。
- サムネイルは単一画像のため、`thumbnailKey` は**省略 = 変更なし**、**現在と同じ既存キーを再送 = 維持**、**`null` = 削除**、**新しい未使用キー = 差し替え**とする。
- 手順は全入れ替えで安定 ID を持たないため、クライアントは画像を残したい手順について既存の `steps[].imageKey` を再送する。`imageKey` が省略または `null` の手順は画像なしになる。
- 指定した画像キーは、リクエストした本人が所有し、かつ未使用または更新対象のこのレシピ自身に既に紐付いているものに限る。他人のキーや別リソースに紐付いているキーは 400。差し替え・削除・手順の全入れ替えで参照から外れた旧画像はストレージ削除ジョブ対象。

### DELETE `/recipes/{id}`（認証必要、本人のみ）

- 204。関連レコード（材料グループ / 材料 / 手順 / お気に入り / 感想 / 通知）を CASCADE 削除。画像はストレージ削除ジョブ対象。
- 自分の他レシピの材料がこのレシピを `ref_recipe_id` で参照していた場合は SET NULL（材料行は残り `ref_recipe_title` が残る）。

### GET `/users/me/recipes`（認証必要）

- 自分の公開 / 非公開レシピ一覧。ページング。
- query `q`（任意）: 自分のレシピをタイトル + 材料名で検索（マッチ規則は [search.md](search.md) と同じ）。材料行の「レシピから選ぶ」ピッカーがこれを使う（他人のレシピを混ぜないため、フィード検索 `GET /recipes?q=` は使わない）。

（フィード用 `GET /recipes` は [home-feed.md](home-feed.md) / [search.md](search.md)）

## 6. バリデーション

上記「入力項目」の表を正とする。サーバーでも同じ制約を適用し、複数エラーをまとめて返す。

## 7. 受け入れ基準

- [ ] 必須項目欠落 / 文字数超過は 400 になる
- [ ] 何人分が未入力・0 以下・範囲外だと 400 になる
- [ ] 材料 0 件 / 手順 0 件 / 材料が 0 のグループがある状態で作成しようとすると 400 になる
- [ ] 他人のレシピを更新 / 削除しようとすると 403 になる
- [ ] レシピ削除時に材料グループ・材料・手順・お気に入り・感想レコードが残らず、画像もストレージから消える（削除ジョブ対象になる）
- [ ] 手順 1 行入力後 `+` を押すと次の行が現れフォーカスが移る
- [ ] 材料・手順の行を削除でき、削除後に順序が詰められる
- [ ] 材料・手順を並べ替えると、その順序で保存・表示される（手順画像も一緒に動く）
- [ ] サムネイル画像を設定 / 変更 / 削除でき、詳細・一覧カードに反映される
- [ ] 各手順に画像を添付 / 削除でき、詳細で本文の下に縦に表示される（カルーセルは無い）
- [ ] `PUT` で `thumbnailKey` を省略するとサムネイルが維持され、`null` で削除、未使用キーで差し替えできる
- [ ] `PUT` で画像を残したい手順の既存 `imageKey` を再送すると画像が維持され、`imageKey` を送らない手順は画像なしになる
- [ ] 空の手順行を残して保存しても、その行は保存されない
- [ ] 非公開レシピは他人の詳細アクセスで見えない
- [ ] 自分のレシピ一覧で非公開に「非公開」バッジが付く
- [ ] 公開レシピを投稿すると、フォロワーに通知が届く
- [ ] 材料が `大さじ 2` / `200 g` / `少々` のように単位ルールどおり表示される
- [ ] 自分のレシピ一覧が無限スクロールで全件たどれる
- [ ] グループを作らずに投稿したレシピが、詳細で見出しなしのフラットな材料リストとして表示される
- [ ] 材料グループを複数作り、名前を付けて、詳細で見出しごとに材料が分かれて表示される
- [ ] 材料行を別グループへドラッグ移動でき、保存・表示に反映される
- [ ] 材料が 0 のグループは保存時に除外される
- [ ] 材料行に自分の別レシピをリンクでき、詳細で材料名タップからそのレシピ詳細へ遷移する
- [ ] 編集中のレシピ自身・他人のレシピを `refRecipeId` に指定すると 400 になる
- [ ] 参照先レシピを削除すると、参照元の材料名は残りリンクが無効化される（タップで「削除されました」）

## 8. 未確定・メモ

- 材料・手順・グループの並べ替え UI（ドラッグ / 上下ボタン）の確定 → [../todo.md](../todo.md)
- グループ数・グループあたり材料数の上限 → [../todo.md](../todo.md)
- 名前なしグループが複数あるときの詳細表示（見出しなしで連結 / 区切り線）→ [../todo.md](../todo.md)
- 「レシピから選ぶ」ピッカーの UI 詳細（一覧 / 検索 / 最近作ったもの）→ [../todo.md](../todo.md)
- グループ名・`ref_recipe_title` を検索対象にするか → [../todo.md](../todo.md)
- 循環参照（A↔B）の表示上の扱い → [../todo.md](../todo.md)
- `PUT` 時の画像キー省略 / `null` の意味は定義済み → [image.md](image.md) / [../todo.md](../todo.md)
- 一時アップロード画像（未参照）の GC → [../todo.md](../todo.md)
- レシピの複製・下書き保存は対象外（将来）
